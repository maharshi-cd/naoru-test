---
- name: Deploy app
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    app_version: "1.0.0"
    deploy_env: "staging"
  tasks:
    - name: Print deploy target
      ansible.builtin.debug:
